# Contributing

Read `AGENTS.md` and `docs/STATUS.md` first. Keep capture and protocol research
offline, bounded, deterministic, redacted, and covered by tests. Never commit
real lab captures, credentials, authorization headers, cookies, private keys,
or recovered secrets. Run `make check`, `make protocol-offline-check`, and
`make docs-check` before proposing changes.
